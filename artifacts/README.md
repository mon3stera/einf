# Artifacts

Store generated architecture diagrams, reports, trace visualizations, and other
reviewable project outputs here. Large raw benchmark samples should stay out of
version control unless deliberately selected as fixtures.
